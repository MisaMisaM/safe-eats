<!DOCTYPE html>
<html>
  <head>
    <title>Extracted Text</title>
  </head>
  <body>
    <h2>Extracted Text</h2>
    <div id="extracted-text-container">
      <% if @extracted_text.present? %>
        <%= @extracted_text %>
        <p>含まれる添加物:
          <% @matching_additives.map { |additive| additive.name }.each do |name| %>
            <%= name %>
          <% end %>
        </p>
      <% else %>
        <p>No text found in the image.</p>
      <% end %>
    </div>
    <a href="/scans/new">Take another picture</a>
  </body>
</html>
