<!DOCTYPE html>
<html>
  <head>
    <title>Extracted Text</title>
  </head>
  <body>
    <h2>Extracted Text</h2>
    <div id="extracted-text-container">
      <% if @extracted_text.present? %>
        <%= @extracted_text %>
        <p>--------------------------------------------</p>
        <div data-refresh-list-target="result">
          <p>＜含まれる添加物＞
            <% @matching_additives.each do |additive| %>
              <div class="card card-trip mb-3 od py-1 px-2" data-controller="enty-thinner" data-action="click->enty-thinner#dropdown">
                <div class="p-3">
                  <div class="overflow-container" data-enty-thinner-target="text">
                    <p class="pt-2 mb-0 fs-5">
                      <i class="fa-solid fa-caret-down"></i>
                      Name: <%= additive.display_name || additive.name %> </p>
                  </div>
                </div>
                <div class="justify-content-center p-3 d-none" data-enty-thinner-target="details">
                  <div class="justify-content-center p-3">
                    <% if additive.information.present? %>
                      <p class="pt-2 mb-2 fs-5"> Information: <%= additive.information %></p>
                    <% end %>
                    <% if additive.purpose.present? %>
                      <p class="pt-2 mb-0 fs-5">Purpose: <%= additive.purpose %></p>
                    <% end %>
                    <% if additive.url.present? %>
                      Reference URL: <a href="<%= additive.url %>" target="_blank"><%= additive.url %></a>
                    <% end %>
                  </div>
                </div>
              </div>
            </div>
          <% end %>
        </p>
        <p>＜アレルギー＞
          <p>※読み取った写真から抽出していますが、実際と異なる場合がありますのでご注意ください</p>
          <div class="icon-inline">
            <% @matching_allergens.map { |allergen| allergen.icon }.uniq.each do |icon| %>
              <%= image_tag icon, class: "icon-smaller" %>
            </p>
          <% end %>
        </div>
      <% else %>
        <p>No text found in the image.</p>
      <% end %>
    </div>
    <a href="/scans/new">Take another picture</a>
  </body>
</html>
